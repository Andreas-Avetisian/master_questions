{
  "name": "master-questions",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "export": "exporter/node_modules/.bin/tsx exporter/src/index.ts",
    "typecheck": "exporter/node_modules/.bin/tsc --noEmit -p exporter",
    "dev": "npm --prefix web run dev",
    "build:web": "npm run export && npm --prefix web run build",
    "check:web": "npm --prefix web run check",
    "test:web": "npm --prefix web run test"
  }
}
